// Declare the variables we intend to use later.
var cats;
var noms;

// Update the treatDispenser function to loop over every animal and check if its name begins with "M".

// If it does, add a message to the results array saying 'No treat for {AnimalName}.'.
// Otherwise, add a message that says '{AnimalName} loved their treat!'
// In both cases, replace {AnimalName} with the actual name of the animal.

// Next, create the function we'll use. This is what you'll modify to make the
// code work.

function treatDispenser (animals) {
  // This array will hold all of the messages we'll return from the function.
  var results = [];

  // Hint: Put your super awesome loop here. :)

  return results;
}

cats = [
  'Jayne',
  'Kaylee',
  'Malcolm',
  'Monty',
  'River',
  'Simon',
  'Zoë'
];

noms = treatDispenser(cats);

console.log(noms);
